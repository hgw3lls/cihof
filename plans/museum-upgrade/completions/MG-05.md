# MG-05 - Interpretive hierarchy, approved text, and visual refinement

Status: Implementation ready for review; editorial and installed-wall sign-off
remain external. MG-06 has not started.

## Worktree

Uncommitted work on `main`, based on
`f47d1d7ff9080072a7f3a28508a73e83907f61a5`. Earlier user changes and MG-00 through
MG-04 remain intact. No commit, push, deployment, or canonical approval changes.

## Implemented

- Nonblocking entry invitation with an optional curator-controlled copy setting.
- Contribution, induction-year, and community discovery using existing explicit
  vocabulary; combined filters, clearing, session reset, and accent-folded search.
- Person and contribution first; full stored biography next; optional reviewed
  Cleveland context and linked archival descriptions; evidence notes last.
- No active runtime biography name-prefixing or sentence-based paragraph repair.
  Stored newlines are preserved. Reviewed generator text fields retain whitespace.
- Fixed the legacy duplicate-heading import case that removed the sentence subject.
- Explicit source/review gate for Cleveland context and per-target scope for
  archival descriptions. No draft content or historical claim was promoted.
- Portrait focal-point propagation, sentence-case reading headings, and long-form
  line height around 1.5 in the existing black/white/red CIHOF design.
- Shared summary helper no longer manufactures shortened prose for the staff
  preview/detail consumers. The legacy living-hall implementation is unchanged.

## Files

Active exhibit component/styles and new `interpretiveModel.ts`; `exhibitCopy.ts`;
data types, normalization, narrative helper, data-utils and prepare-data;
generated runtime/fallback feeds; interpretation tests and updated heading/reset
assertions; capture script, package command, ignore rule, workflow and tracker.
The overall Git diff also includes earlier stages; it is not an MG-05-only diff.

## Verification

- `npm run typecheck`: passed.
- `npm run validate:entities`: passed, 544 entities / 2041 relationships, no errors.
- `npm run validate:kiosk`: passed, 111/111 portraits wall-ready.
- `npm run build:kiosk`: passed.
- `npm run build:public`: passed again after testing, without test-only settings;
  final `dist/` is the public artifact, not the restricted kiosk build.
- `npm run build:portal`: passed; separate portal suite: 2/2 passed.
- Focused interpretation suite: 8/8 passed after correcting test assumptions
  about substring matching and nested name markup.
- Final full visitor suite: 45/45 passed on port 4176. Earlier runs exposed a
  test-authoring assertion about substring matches/nested text; corrected and
  verified in both the focused and full reruns.
- Direct canonical/generator equality check: 456 populated text fields across
  111 people match exactly. Canonical files under `data/` are unchanged.
- `npm run mg05:capture`: 16 images, no page/console errors or horizontal overflow.
  Desktop 1920x1080 and phone 390x844, light/dark, neutral/filtered/selected/record.
  Real fonts awaited; representative screenshots visually inspected.
- `git diff --check`: passed.

## Evidence

Local ignored evidence: `artifacts/mg05-review/manifest.json` and 16 PNG files.
Reproduce with `npm run preview -- --host 127.0.0.1 --port 4175 --strictPort`
and `npm run mg05:capture` after a public build.

## Existing Blockers

`npm run validate:media-clearance` still fails with 93 pending video clearances;
zero schema errors. No approval flags were changed to make the gate pass.
Node 26.8.1 was available, not the pinned Node 22; rerun release checks under Node 22.

## Review Dependencies

Curators must review the invitation, vocabulary semantics, existing summaries,
optional Cleveland context, source references, paragraph migrations, and portrait
crops. All existing archive leads remain staff-review material. Unknown rights
scope stays unpublished. Do not confuse explicit provenance with editorial
approval of new interpretation.

Raw CSV import normalization remains a legacy boundary: exact-rendering evidence
does not certify byte equality with original harvested HTML. Many stored profiles
are still a single long paragraph; changing that structure requires review.
No physical-wall, assistive-technology, or endurance certification was performed.
MG-06 covers accessibility; MG-08 still owns the broader release artifact audit.

## Rollback and Next Task

Rollback only MG-05 edits using reviewed file hunks, then regenerate data and
rebuild. Do not reset the mixed worktree or discard earlier stages/user work.
After review, the next bounded stage is MG-06: accessibility, reachable operation,
and continuation. This turn stops before that stage.
