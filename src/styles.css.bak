:root {
  color-scheme: dark;
  font-family: 'Segoe UI', system-ui, sans-serif;
  background-color: #0b0f16;
  color: #f6f7fb;
}

* {
  box-sizing: border-box;
}

body {
  margin: 0;
  min-height: 100vh;
  background: #0b0f16;
}

h1,
h2,
h3 {
  margin: 0;
}

.app-state {
  display: grid;
  place-items: center;
  min-height: 100vh;
  font-size: 1.4rem;
}

.app-state--error {
  color: #ff9a9a;
}

.app-shell {
  min-height: 100vh;
}

.mode-menu-bar {
  position: sticky;
  top: 0;
  z-index: 40;
  display: flex;
  justify-content: center;
  gap: 16px;
  padding: 12px 16px;
  background: #0b0f16;
  border-bottom: 1px solid #1f2937;
}

.mode-menu-button {
  min-height: 52px;
  min-width: 160px;
  padding: 12px 20px;
  border-radius: 16px;
  border: 2px solid #2b364a;
  background: #111827;
  color: #f6f7fb;
  font-size: 1.1rem;
  font-weight: 600;
  letter-spacing: 0.02em;
  touch-action: manipulation;
}

.mode-menu-button--active {
  background: #2743ff;
  border-color: #4b63ff;
  color: #ffffff;
  box-shadow: 0 0 0 4px rgba(39, 67, 255, 0.35);
}

.mode-badge {
  position: fixed;
  top: 12px;
  right: 12px;
  background: rgba(39, 67, 255, 0.85);
  color: #ffffff;
  padding: 6px 12px;
  border-radius: 999px;
  font-size: 0.9rem;
  z-index: 30;
}

.timeline-view,
.explore-view,
.hub-map-view {
  padding: 24px 32px 48px;
  display: flex;
  flex-direction: column;
  gap: 24px;
}

.view-header {
  display: flex;
  align-items: center;
  justify-content: space-between;
  gap: 24px;
  flex-wrap: wrap;
}

.view-header h1 {
  font-size: 2.4rem;
}

.subtitle {
  font-size: 1.1rem;
  color: #a9b2c4;
  margin-top: 4px;
}

.search-box {
  display: flex;
  flex-direction: column;
  gap: 6px;
  min-width: 240px;
}

.search-box input,
.control-group input,
.control-group select {
  background: #111827;
  border: 1px solid #1f2937;
  border-radius: 12px;
  padding: 12px 16px;
  font-size: 1rem;
  color: #f6f7fb;
  min-height: 48px;
}

.explore-controls {
  display: flex;
  flex-wrap: wrap;
  gap: 16px;
  align-items: flex-end;
}

.hub-map-show-all {
  min-height: 52px;
  min-width: 200px;
  padding: 12px 20px;
  border-radius: 16px;
  border: none;
  background: #2743ff;
  color: #ffffff;
  font-size: 1.05rem;
  font-weight: 600;
}

.hub-map-layout {
  display: grid;
  grid-template-columns: 1fr;
  gap: 24px;
  align-items: stretch;
}

.hub-map-layout--drawer-open {
  grid-template-columns: minmax(0, 1.5fr) minmax(320px, 0.9fr);
}

.hub-map-visual {
  min-height: 520px;
  background: #0f172a;
  border: 1px solid #1f2937;
  border-radius: 24px;
  overflow: hidden;
}

.hub-map-svg {
  width: 100%;
  height: 100%;
}

.hub-map-rays path {
  stroke: rgba(76, 109, 255, 0.45);
  stroke-width: 3;
  fill: none;
}

.hub-map-region {
  cursor: pointer;
}

.hub-map-region circle {
  fill: #111827;
  stroke: #2b364a;
  stroke-width: 3;
}

.hub-map-region--active circle {
  fill: #1d33c7;
  stroke: #8ea0ff;
  filter: drop-shadow(0 0 14px rgba(76, 109, 255, 0.6));
}

.hub-map-label {
  fill: #f6f7fb;
  font-size: 15px;
  font-weight: 600;
  text-anchor: middle;
  dominant-baseline: middle;
}

.hub-map-count {
  fill: #c1c9da;
  font-size: 12px;
  text-anchor: middle;
  dominant-baseline: middle;
}

.hub-map-hub {
  cursor: pointer;
}

.hub-map-hub circle {
  fill: url(#hubGlow);
  stroke: #7dd3fc;
  stroke-width: 3;
}

.hub-map-hub-label {
  fill: #ffffff;
  font-size: 18px;
  font-weight: 700;
  text-anchor: middle;
  dominant-baseline: middle;
}

.hub-map-hub-subtitle {
  fill: #cfe5ff;
  font-size: 12px;
  text-anchor: middle;
  dominant-baseline: middle;
}

.hub-map-drawer {
  display: none;
  flex-direction: column;
  gap: 16px;
  background: #0f172a;
  border: 1px solid #1f2937;
  border-radius: 24px;
  padding: 20px;
  max-height: calc(100vh - 200px);
}

.hub-map-drawer--open {
  display: flex;
}

.hub-map-drawer__header {
  display: flex;
  align-items: center;
  justify-content: space-between;
  gap: 16px;
}

.hub-map-drawer__subtitle {
  color: #a9b2c4;
  margin-top: 6px;
}

.hub-map-drawer__close {
  min-height: 48px;
  min-width: 120px;
  border-radius: 16px;
  border: none;
  background: #1f2937;
  color: #f6f7fb;
  font-size: 1rem;
}

.hub-map-drawer__all {
  min-height: 48px;
  min-width: 180px;
  border-radius: 16px;
  border: none;
  background: #1f2937;
  color: #f6f7fb;
  font-size: 1rem;
  font-weight: 600;
}

.hub-map-drawer__controls {
  display: grid;
  grid-template-columns: repeat(auto-fit, minmax(200px, 1fr));
  gap: 12px;
  align-items: end;
}

.hub-map-drawer__content {
  overflow-y: auto;
  padding-right: 8px;
  display: flex;
  flex-direction: column;
  gap: 16px;
  flex: 1;
}

.hub-map-group {
  display: flex;
  flex-direction: column;
  gap: 12px;
}

.hub-map-group__title {
  position: sticky;
  top: 0;
  background: #0f172a;
  padding: 8px 0;
  font-size: 1.1rem;
  z-index: 2;
}

.hub-map-group__grid {
  display: grid;
  grid-template-columns: repeat(auto-fit, minmax(210px, 1fr));
  gap: 16px;
}

.hub-map-drawer__footer {
  display: flex;
  justify-content: center;
}

.hub-map-drawer__footer button {
  min-height: 48px;
  min-width: 160px;
  border-radius: 16px;
  border: none;
  background: #2743ff;
  color: #ffffff;
  font-size: 1.05rem;
}

.control-group {
  display: flex;
  flex-direction: column;
  gap: 6px;
  min-width: 220px;
}

.year-scrubber {
  display: flex;
  align-items: center;
  gap: 12px;
}

.year-scrubber-track {
  display: flex;
  gap: 12px;
  overflow-x: auto;
  scrollbar-width: none;
  padding: 6px 4px;
  flex: 1;
}

.year-scrubber-track::-webkit-scrollbar {
  display: none;
}

.scrubber-nudge {
  min-height: 48px;
  min-width: 48px;
  border-radius: 16px;
  border: none;
  background: #1b2432;
  color: #f6f7fb;
  font-size: 1.3rem;
}

.year-chip,
.region-chip {
  min-height: 48px;
  padding: 10px 18px;
  border-radius: 999px;
  border: 1px solid #2b364a;
  background: #121a27;
  color: #d8dee9;
  font-size: 1.05rem;
  white-space: nowrap;
}

.year-chip--active,
.region-chip--active {
  background: #2743ff;
  border-color: #2743ff;
  color: #ffffff;
  box-shadow: 0 0 0 4px rgba(39, 67, 255, 0.3);
}

.region-filters {
  display: flex;
  flex-wrap: wrap;
  gap: 12px;
}

.inductee-grid {
  display: grid;
  grid-template-columns: repeat(auto-fit, minmax(220px, 1fr));
  gap: 18px;
}

.inductee-card {
  background: #111827;
  border: 1px solid #1f2937;
  border-radius: 20px;
  padding: 12px;
  display: flex;
  flex-direction: column;
  gap: 12px;
  color: inherit;
  text-align: left;
  min-height: 300px;
}

.inductee-card__image {
  background: #1f2937;
  border-radius: 16px;
  overflow: hidden;
  aspect-ratio: 3 / 4;
}

.inductee-card__image img {
  width: 100%;
  height: 100%;
  object-fit: cover;
  display: block;
}

.inductee-card__body h3 {
  font-size: 1.1rem;
  margin-bottom: 6px;
}

.inductee-card__body p {
  margin: 0;
  color: #b4bdcf;
}

.empty-state {
  padding: 32px;
  font-size: 1.1rem;
  color: #b4bdcf;
}

.modal-backdrop {
  position: fixed;
  inset: 0;
  background: rgba(3, 7, 18, 0.75);
  display: grid;
  place-items: center;
  padding: 24px;
  z-index: 20;
}

.modal {
  background: #0f172a;
  border-radius: 24px;
  padding: 28px;
  width: min(1100px, 95vw);
  max-height: 92vh;
  overflow-y: auto;
  position: relative;
}

.modal-close {
  position: absolute;
  top: 16px;
  right: 16px;
  min-height: 44px;
  min-width: 44px;
  border-radius: 16px;
  border: none;
  background: #1f2937;
  color: #f6f7fb;
  font-size: 1rem;
  padding: 0 16px;
}

.modal-header {
  margin-bottom: 18px;
  display: flex;
  justify-content: space-between;
  gap: 16px;
  flex-wrap: wrap;
}

.modal-meta {
  color: #b4bdcf;
  margin: 4px 0;
}

.modal-content {
  display: grid;
  grid-template-columns: minmax(220px, 1fr) minmax(280px, 1.2fr);
  gap: 24px;
  align-items: start;
}

.media-gallery img {
  width: 100%;
  border-radius: 18px;
  border: 1px solid #1f2937;
}

.carousel-controls {
  display: flex;
  align-items: center;
  justify-content: space-between;
  margin-top: 12px;
  gap: 12px;
}

.carousel-controls button {
  min-height: 44px;
  min-width: 120px;
  border-radius: 12px;
  border: none;
  background: #1f2937;
  color: #f6f7fb;
  font-size: 1rem;
}

.modal-bio {
  max-height: 320px;
  overflow-y: auto;
  padding-right: 12px;
}

.modal-bio p {
  line-height: 1.5;
  color: #d0d6e4;
}

.modal-videos {
  margin-top: 24px;
}

.video-grid {
  display: grid;
  grid-template-columns: repeat(auto-fit, minmax(260px, 1fr));
  gap: 16px;
}

.video-grid iframe {
  width: 100%;
  aspect-ratio: 16 / 9;
  border: none;
  border-radius: 12px;
  background: #111827;
}

.video-grid video {
  width: 100%;
  border-radius: 12px;
  background: #111827;
}

.video-link {
  display: inline-flex;
  align-items: center;
  justify-content: center;
  min-height: 44px;
  padding: 10px 16px;
  border-radius: 12px;
  background: #1f2937;
  color: #f6f7fb;
  text-decoration: none;
  font-weight: 600;
}

.profile-link {
  display: inline-flex;
  align-items: center;
  justify-content: center;
  min-height: 44px;
  padding: 10px 18px;
  border-radius: 999px;
  background: #1f2937;
  color: #f6f7fb;
  text-decoration: none;
  font-weight: 600;
}

.load-more {
  display: flex;
  justify-content: center;
}

.load-more button {
  min-height: 48px;
  min-width: 160px;
  border-radius: 16px;
  border: none;
  background: #2743ff;
  color: #ffffff;
  font-size: 1.05rem;
}

@media (max-width: 900px) {
  .modal-content {
    grid-template-columns: 1fr;
  }
}

@media (max-width: 1100px) {
  .hub-map-layout--drawer-open {
    grid-template-columns: 1fr;
  }

  .hub-map-drawer {
    max-height: none;
  }
}

body.timeline-brutalist-root,
body.timeline-brutalist-root #root {
  height: 100%;
  overflow: hidden;
  overscroll-behavior: none;
}

body.timeline-brutalist-root {
  margin: 0;
}

body.timeline-brutalist-root .app-shell {
  height: 100%;
  display: flex;
  flex-direction: column;
}

body.timeline-brutalist-root .timeline-view {
  flex: 1;
  min-height: 0;
}

.timeline-brutalist {
  --bg: #ffffff;
  --fg: #000000;
  --accent: #ff2d00;
  --border: #000000;
  --muted: #2f2f2f;
  background: var(--bg);
  color: var(--fg);
  display: grid;
  grid-template-rows: 88px 84px 1fr;
  gap: 0;
  padding: 0;
  height: 100%;
  font-family: 'Space Mono', 'Fira Mono', 'Segoe UI', system-ui, sans-serif;
  letter-spacing: 0.02em;
}

.timeline-top-bar {
  display: grid;
  grid-template-columns: minmax(220px, 1fr) minmax(280px, 1.4fr) minmax(220px, 1fr);
  align-items: center;
  padding: 0 24px;
  border-bottom: 4px solid var(--border);
  text-transform: uppercase;
}

.timeline-top-bar__title h1 {
  font-size: 1.5rem;
  font-weight: 800;
}

.timeline-top-bar__title p {
  margin: 6px 0 0;
  font-size: 0.85rem;
  font-weight: 700;
}

.timeline-top-bar__status {
  display: flex;
  justify-content: center;
  align-items: baseline;
  gap: 12px;
  font-size: 1.05rem;
}

.timeline-top-bar__status strong {
  font-size: 1.5rem;
  font-weight: 800;
}

.timeline-top-bar__actions {
  display: flex;
  justify-content: flex-end;
  gap: 12px;
}

.timeline-top-bar__actions button {
  min-height: 48px;
  padding: 0 18px;
  border: 4px solid var(--border);
  background: var(--bg);
  color: var(--fg);
  font-weight: 800;
  text-transform: uppercase;
}

.year-menu {
  display: flex;
  align-items: center;
  gap: 10px;
  padding: 10px 24px;
  border-bottom: 4px solid var(--border);
  background: var(--bg);
  overflow-x: auto;
  overflow-y: hidden;
  scrollbar-width: thin;
  touch-action: pan-x;
}

.year-menu::-webkit-scrollbar {
  height: 8px;
}

.year-menu::-webkit-scrollbar-thumb {
  background: var(--border);
}

.year-menu__button {
  min-width: 72px;
  padding: 12px 16px;
  border: 4px solid var(--border);
  background: var(--bg);
  color: var(--fg);
  font-weight: 800;
  letter-spacing: 0.08em;
  text-transform: uppercase;
}

.year-menu__button--active {
  background: var(--accent);
  color: #ffffff;
}

.timeline-scroller {
  overflow-x: auto;
  overflow-y: hidden;
  scroll-snap-type: x mandatory;
  scrollbar-width: thin;
  overscroll-behavior-x: contain;
  overscroll-behavior-y: none;
  touch-action: pan-x;
  min-height: 0;
}

.timeline-scroller::-webkit-scrollbar {
  height: 12px;
}

.timeline-scroller::-webkit-scrollbar-thumb {
  background: var(--border);
}

.timeline-track {
  display: flex;
  align-items: stretch;
  gap: 40px;
  padding: 24px 40px 32px;
  min-height: 100%;
  height: 100%;
<<<<<<< ours
=======
}

.timeline-spacer {
  flex: 0 0 clamp(0px, calc(50vw - 240px), 50vw);
  height: 1px;
>>>>>>> theirs
}

.year-stop {
  min-width: clamp(420px, 70vw, 760px);
  border: 4px solid var(--border);
  padding: 24px;
  background: var(--bg);
  display: grid;
  grid-template-rows: auto 1fr;
  gap: 18px;
  scroll-snap-align: center;
}

.year-stop__marker {
  display: flex;
  align-items: center;
  gap: 18px;
}

.year-stop__year {
  font-size: 2.6rem;
  font-weight: 900;
  text-transform: uppercase;
}

.year-stop__tick {
  width: 8px;
  height: 72px;
  background: var(--border);
}

.year-stop__gallery {
  max-height: 0;
  opacity: 0;
  transform: translateY(-6px);
  transition: max-height 200ms ease, opacity 200ms ease, transform 200ms ease;
  overflow: hidden;
}

.year-stop--active .year-stop__gallery {
  max-height: 420px;
  opacity: 1;
  transform: translateY(0);
}

.year-stop__gallery-inner {
  display: flex;
  gap: 16px;
  overflow-x: auto;
  overflow-y: hidden;
  padding-bottom: 8px;
  scrollbar-width: thin;
}

.year-stop__gallery-inner::-webkit-scrollbar {
  height: 8px;
}

.year-stop__gallery-inner::-webkit-scrollbar-thumb {
  background: var(--border);
}

.inductee-thumb {
  min-width: 160px;
  max-width: 180px;
  border: 4px solid var(--border);
  background: var(--bg);
  color: var(--fg);
  padding: 10px;
  display: grid;
  gap: 10px;
  text-align: left;
  font-weight: 800;
}

.inductee-thumb img {
  width: 100%;
  height: 160px;
  object-fit: cover;
  border: 3px solid var(--border);
  background: #ffffff;
}

.inductee-thumb span {
  font-size: 0.85rem;
  letter-spacing: 0.06em;
}

.timeline-brutalist .modal {
  border-radius: 0;
  border: 4px solid var(--border);
}

.timeline-brutalist .modal-close {
  border-radius: 0;
  border: 3px solid var(--border);
  text-transform: uppercase;
}

.timeline-brutalist .modal-content {
  max-height: 60vh;
  overflow-y: auto;
}

.timeline-brutalist .media-gallery img {
  border-radius: 0;
  border: 3px solid var(--border);
}

.timeline-brutalist .carousel-controls button {
  border-radius: 0;
  border: 3px solid var(--border);
  background: var(--bg);
  color: var(--fg);
  font-weight: 800;
  text-transform: uppercase;
}

.timeline-brutalist .modal-videos {
  margin-top: 0;
}

.timeline-brutalist .video-grid video,
.timeline-brutalist .video-grid iframe {
  border-radius: 0;
  border: 3px solid var(--border);
  background: #ffffff;
}

@media (max-width: 1100px) {
  .timeline-top-bar {
    grid-template-columns: 1fr;
    height: auto;
    padding: 16px;
    text-align: left;
    gap: 12px;
  }

  .timeline-top-bar__status {
    justify-content: flex-start;
  }

  .timeline-top-bar__actions {
    justify-content: flex-start;
    flex-wrap: wrap;
  }

  .timeline-track {
    padding: 20px;
    gap: 24px;
  }

  .year-stop {
    min-width: 80vw;
  }
}
