# MG-06 - Accessibility, reachable operation, and continuation

Status: Software implementation ready for review. Installation and external
continuation sign-off remain blocked as described below. MG-07 is not started.

## Worktree

Uncommitted `main` based on `f47d1d7ff9080072a7f3a28508a73e83907f61a5`.
Earlier stages and user edits remain intact. No commit, deployment, permission
change, canonical content change, or media/rights approval.

## Implemented Behavior

- Shared native modal component for QR/admin, inert background, initial focus,
  Tab/Shift+Tab containment, Escape isolation, and surviving-trigger restoration.
- QR-local Start over and in-dialog session warning/extension controls preserve
  reset behavior while the underlying exhibit is inert.
- Skip-to-content, scroll-region keyboard access, focus recovery after clear/back,
  surviving record trigger fallback, film Escape focus restoration, connection-list
  shortcut, and visible focus for links/headings.
- Existing chronology buttons and keyboard commands remain non-drag alternatives.
- Reduced-motion CSS, enlarging masthead rows, scrollable sidebar, stale-person
  feedback, and a nondefault `?kiosk=1&reach=1` lower-screen prototype.
- Continuation URLs restricted to canonical institution profile hosts, with no
  credentials, custom ports, staff routes, query/session state, or fragments.
  Nested historical paths and missing trailing slashes remain supported.
- Added development-only axe, PNG parsing, and QR-decoding test dependencies.

## Files

`src/components/Modal.tsx`; active `ArchiveExhibit`, `LinksScene`, `YearsScene`,
and exhibit CSS; shared `AdminDataPanel`; continuation model; accessibility tests
and one modal-scoped reset assertion; capture script, package/lockfile, ignore
rule, installation review, tracker, and this record. The total Git diff also
contains earlier stages and is not an MG-06-only patch.

## Verification

- `npm run typecheck`: passed.
- `npm run validate:entities`: passed, 544 entities and 2041 relationships.
- `npm run validate:kiosk`: passed; all 111 portraits wall-ready.
- `npm run build:kiosk`: passed.
- Earlier targeted tests: 13/14 passed; reset assertion updated to address the
  reset action inside the now-modal QR instead of matching the inert header too.
- Earlier full run: 55/56 passed; exposed admin autofocus stealing the return
  target. Fixed by sequencing initial focus after the modal captures its trigger.
- Full visitor regression suite: 57/57 passed.
- Final focused accessibility, admin, and session suite: 18/18 passed after the
  narrow-screen brand and in-dialog warning layout refinements.
- Separate staff portal build and tests: passed, 2/2 tests.
- Final `npm run build:public`: passed without test-only settings. `dist/` is
  the public artifact, and `dist-portal/` remains the separate staff artifact.
- Automated accessibility checks passed in both themes for the tested scenes,
  full record, and QR. These results do not certify all content or AT behavior.
- QR pixels decoded to the canonical Alex Machaskee public profile. Private,
  credential-bearing and staff paths are rejected; session/hash state is stripped.
- Font-loaded captures at 1920x1080, 960x540, and 320x700 include record, reading,
  QR, selected Links, and Years in both themes. Enlarged-text and lower-screen
  prototype captures are included. Initial 200% text screenshot exposed clipped
  branding; masthead rows were fixed and a geometric regression assertion added.
- Final public-build capture refresh: 30 state captures with zero browser errors
  or page overflow, plus lower-screen and 200% text views. Refreshed narrow and
  enlarged-text screenshots were visually inspected.
- `git diff --check`: passed. Canonical `data/` files are unchanged.

## Evidence

Local ignored `artifacts/mg06-review/`: `manifest.json`, 30 state screenshots,
`text-200-percent.png`, `reachable-prototype.png`, and the optional live public
phone screenshot/JSON. Tests also exercise full Tab cycles, Escape, focus return,
stale links, reduced motion, 320px reflow, and doubled computed text sizes.

## Release Blockers and Limits

1. Live external profile: HTTP 200 and matching person heading on 2026-09-20,
   but a 700px document overflows the 390px phone client viewport. The institution's
   site owner must fix reflow or authorize a tested alternative public route.
   One remote page was checked, not all 111. No physical camera scan was performed.
2. Physical dimensions, mounting height, OS scaling, shelf/input placement,
   approach/clearance, and seated/standing reach remain unknown. Requested from
   the user; see the installation review checklist.
3. VoiceOver/NVDA/TalkBack and an institution-supported independent blind-visitor
   route still require human testing and approval. Phone ownership is not a fallback.
4. Installed Node 22.22.3 cannot start: Homebrew `libsimdutf.34.dylib` is missing.
   Checks used working Node 26.8.1. Node 22 release verification remains open;
   the system runtime was not repaired or replaced.
5. Existing full media gate still reports 93 pending video clearances, zero schema
   errors. This stage does not authorize approving or publishing those films.

Viewport geometry and programmatic text doubling do not certify actual browser
zoom, physical targets, assistive technology, or installation accessibility.
The graph has a linear list alternative; the two-dimensional map is not claimed
to become a one-column reading view.

## Rollback and Next Task

Revert only reviewed MG-06 hunks and new files, restore the matching package lock,
then regenerate/build. Do not reset this mixed worktree or discard earlier work.
The next bounded software stage is MG-07 (offline package, updates, recovery),
after review. The external accessibility blockers remain release blockers even
if that software stage proceeds.
