# MG-01 completion record

Task: MG-01 - Inclusive collection and stable visitor context

Commit/worktree: `main` at `f47d1d7ff9080072a7f3a28508a73e83907f61a5`; the worktree also retains the pre-existing visitor changes documented by MG-00.

Implemented behavior: The public runtime bundle is treated as the publication boundary, and valid people are no longer excluded because a portrait is absent. Missing, failed, and undersized portraits use an accessible initials fallback without requesting an empty URL. People, Links, Years, and deep links retain the same canonical person IDs. A selected People tile remains in place with an `IN FOCUS` treatment. Closing a full record preserves scene state, query, sort, selection, and scroll, then restores focus to the originating control or a stable replacement. The initial collection remains unselected. No canonical content, approval status, or media-rights decision changed.

Files changed by MG-01:

- `.gitignore`
- `package.json`
- `src/features/archive-exhibit/ArchiveExhibit.tsx`
- `src/features/archive-exhibit/LinksScene.tsx`
- `src/features/archive-exhibit/YearsScene.tsx`
- `src/features/archive-exhibit/archive-exhibit.css`
- `src/features/archive-exhibit/archiveModel.ts`
- `tests/index-experience.spec.ts`
- `scripts/capture-mg01-review.mjs`
- `cihof-museum-upgrade/TASK_TRACKER.md`
- `cihof-museum-upgrade/completions/MG-01.md`

Tests/commands actually run and outcomes:

- `npm run typecheck`: pass.
- `npm run validate:entities`: pass, 544 entities and 2,041 relationships, no errors or warnings.
- `npm run build:public`: pass.
- `npm run test:kiosk` against the verified local preview: 17/17 pass. New coverage includes no-image and broken-image fixtures, a long name, selected search result retention, deep-linked no-image selection across all scenes, touch and keyboard selection, and far-down record return with focus/scroll restoration.
- `npm run validate:kiosk`: pass; the generated wall-profile report was then restored to the repository's full-profile report by rerunning full validation.
- `npm run media:validate -- --strict --profile=full`: expected failure, 93 pending film assets; this restores the tracked full-profile report and is not an MG-01 regression.
- `npm run build:portal`: pass.
- `npm run test:portal`: the first attempt reused port 4175, which was serving the visitor build, so both tests failed before reaching portal UI. The controlled rerun on port 4177 passed 2/2.
- `npm run mg01:capture`: pass, three screenshots and zero console/page errors.
- `git diff --check`: pass.

Evidence files:

- `artifacts/mg01-review/manifest.json`
- `artifacts/mg01-review/1920x1080-selected-person-desktop.png`
- `artifacts/mg01-review/390x844-no-image-deep-link-mobile.png`
- `artifacts/mg01-review/1920x1080-broken-image-long-name-desktop.png`
- `tests/index-experience.spec.ts`

Existing failures versus introduced failures: Full media clearance still fails for 93 pending films, and MG-00's offline, content-approval, wall-position, and hardware blockers remain. The initial portal smoke failure was caused by an occupied local port serving the wrong application; the isolated rerun passed. No introduced product failure was found.

Curatorial or hardware dependencies: Publication remains bounded by the prepared public runtime data and its existing editorial process. Film rights/captions/transcripts, relationship approvals, wall positions, and installed-device acceptance remain outside MG-01. Physical touch-wall reach and real assistive-technology behavior were not tested.

Remaining risks: All 111 current runtime people have primary portrait paths, so missing and failed media behavior is verified with routed fixtures rather than current canonical records. The run used Node 26.8.1 instead of the repository's pinned Node 22. Production-origin and installed-device behavior remain untested.

Rollback approach: Revert the MG-01 visitor/model/style/test changes and remove the `mg01:capture` script entry and capture script. The ignored screenshots can be removed independently. Canonical data requires no rollback.

Next bounded task: MG-02 - Authoritative reset and accessible sessions. Stop for review before starting it.
