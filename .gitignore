node_modules/
dist/
dist-portal/
.env
.env.local

# OS/editor/build artifacts
.DS_Store
*.bak
*.bak.*
*.tsbuildinfo

# Local generated audit output
plans/artifacts/data-audit.local.json
plans/artifacts/launch-readiness.local.json
plans/artifacts/video-caption-transcript-report*.csv
plans/artifacts/video-caption-transcript-report*.json
plans/artifacts/video-file-download-queue.csv
plans/artifacts/video-file-download-queue.json
plans/artifacts/video-file-sync-report*.csv
plans/artifacts/video-file-sync-report*.json
plans/artifacts/video-manifest-normalization-report*.json
plans/artifacts/video-whisper-transcription-report*.json

# Local safety copies written by scripts/apply-media-decisions.js before it
# edits the manifest. Useful on the machine that made the change, noise in the
# repository — and one reached main before this rule existed.
data/*.backup-*

# Local downloaded video payloads are too large for normal Git storage:
# 633 MP4s, 41 GB. They stay out.
#
# The captions, posters and transcripts sit in the same folder and are 11 MB,
# and they are required — a film is not published without approved captions and
# a transcript, whichever player serves the picture. The 279 files the media
# manifest references are tracked by an explicit `git add -f`, which this
# pattern cannot undo. A NEW film's assets will not be picked up automatically:
# add them the same way. `npm run media:assert` fails when one is missing.
public/videos/
public/media/videos/*
!public/media/videos/.gitkeep
data/media-acquisition/youtube-download-archive.txt
tools/whisper-models/*.bin
plans/artifacts/whisper-audio-cache/

# Local staff portal runner scratch files
.portal/

# Local Playwright artifacts
test-results/
playwright-report/
plans/artifacts/mg00-baseline/
plans/artifacts/mg01-review/
plans/artifacts/mg02-review/
plans/artifacts/mg03-review/
plans/artifacts/mg04-review/
plans/artifacts/mg05-review/
plans/artifacts/mg06-review/
plans/artifacts/ce00-baseline/
dist-kiosk/

# Rewrite build output
apps/*/dist/
apps/web/public/media/
apps/exhibit/public/data/
apps/exhibit/test-results/
apps/web/test-results/

# Local reports written by the data scripts
/reports/
