# EV-00 completion record

**Status:** Complete
**Code commit or working-tree baseline:** `sota` at `d97afd32b967a5b6e62ad6075b133979e8468b0a`; all CE and EV work remains uncommitted
**Build target and content revision:** public and portal; `b81f21754dac02d1d10de0a78dcd7419d6975430d463254e88000ad7bafa4fd8`; runtime schema 2
**Stage scope:** adopt this plan, state its boundary against MG and CE, change no code path

## Changes and rationale

This stage is adoption and boundary-setting. It adopts the refreshed baseline
measured on 2026-09-21 rather than repeating the measurement, and adds this
plan's own statement of what it supersedes.

No code path, canonical data, generated data, publication decision or rights
status was changed by this stage.

## Files changed

The plan itself: `README.md`, `AUDIT.md`, `PLAN.md`, `DECISIONS.md`,
`ACCEPTANCE.md`, `TRACKER.md`, `stages/EV-00`–`EV-09`, `templates/COMPLETION.md`,
and this record.

## Baseline adopted

`cihof-city-experience/BASELINE_CURRENT.md` and `completions/CE-00.md`, refreshed
2026-09-21 against the current tree. Accurate and not re-measured here.

## Dirty-tree boundary

`HEAD` is unchanged at the audited commit. Nothing is committed. The tree
divides by stage:

| Owner | Paths |
|---|---|
| CE-00 | `.gitignore`, `scripts/capture-mg00-baseline.mjs`, `cihof-city-experience/` |
| CE-01 | `src/data/types.ts`, `publicationPolicy.ts`, `relationshipPublication.ts`, `runtimeDataBundle.ts`, `archiveModel.ts`, `interpretiveModel.ts`, `scripts/prepare-data.js`, `report-city-content-migration.mjs`, `vite.config.ts`, `tsconfig.json`, `package.json`, `data/cihof_places.json`, `public/data/*`, `tests/fixtures/`, `tests/publication-contract.spec.ts` |
| CE-02 | `state/`, `ArchiveExhibit.tsx`, `LinksScene.tsx`, `archive-exhibit.css`, `tests/exhibit-state.spec.ts` |
| EV (this plan) | `cihof-evidence-led-plan/` |

A copy of the uncommitted tree was archived outside the repository before any
of this work began.

## MG crosswalk

MG-00–04 Complete and retained. MG-05/06 Ready for review; nothing here closes
their curatorial, mobile, physical-reach or assistive-technology sign-offs.
MG-07 and MG-08 Not started; their applicable work lands in EV-03 and EV-09 and
their external sign-offs are carried, not closed.

## CE crosswalk

CE-00 and CE-01 are Complete and this plan builds on them. CE-01's content
revision matches this record and its contract suite passes; its entity and
relationship gates were verified rule-preserving on current data.

CE-02 is in progress in the working tree. Its three known defects — the Links
constellation destroyed by the shared discovery query, the single-valued
`detail` union discarding an active film, and the suite that collected zero
tests — are fixed and verified. The rest of CE-02's scope (grouping, comparison
IDs, time mode, place focus, feature flags) is not done.

CE-03 through CE-11 remain valid as **destinations**. Their **order** is
superseded by decision E03: surfaces are built when reviewed content clears a
stated threshold. CE-12, added during the source audit, is folded into EV-02.

## Acceptance cases exercised

The stage's own acceptance only: boundary, crosswalk and supersession are
explicit and checkable; no code path changed. No numbered T/P/A/O case is
claimed by this stage.

## Commands actually run

| Command | Exit/result | Collected count | Evidence path |
|---|---|---|---|
| none | This stage writes records and changes no code | n/a | n/a |

The measurements this record relies on were run during the CE-00 refresh and the
source audit, and are cited there rather than re-run.

## Existing failures versus introduced regressions

None introduced. Carried forward: the pinned Node 22 runtime cannot start, and a
Homebrew `simdutf` upgrade to 9.2.0 on 2026-09-21 also broke Node 26 by
orphaning `merve`'s link to `libsimdutf.35`. A `brew reinstall simdutf node@22`
ran for roughly 80 minutes, failed, and rolled back to node@22 22.22.3. All
work since has run on Node 26.8.1 with the genuine simdutf 9.1.0 library
supplied from Homebrew's own download cache via `DYLD_FALLBACK_LIBRARY_PATH`.
That is a local workaround for this session, not a repaired runtime, and it is
not a Node 22 pass.

## Not performed / blocked

No build, test, validator or capture was run by this stage. Node 22 verification
remains blocked.

## Content or rights changes

None. No approval was applied or inferred.

## Visual, accessibility and physical review

None performed by this stage.

## Release and compatibility impact

None. `HEAD` unchanged; no artifact rebuilt by this stage.

## Next-stage readiness

EV-01 may proceed. It owns the suite-integrity guard and the order-dependent
focus flake, and every later stage depends on its outcome.

Do not push, merge, deploy or mark installation approved through this record.
