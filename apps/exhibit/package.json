{
  "name": "@cihof/exhibit",
  "version": "0.1.0",
  "private": true,
  "type": "module",
  "description": "The installed exhibit. A long-running, offline-capable touch surface.",
  "dependencies": {
    "@cihof/content": "*",
    "@cihof/pipeline": "*",
    "qrcode": "^1.5.4"
  },
  "devDependencies": {
    "@types/react": "^18.3.29",
    "@types/react-dom": "^18.3.7",
    "@vitejs/plugin-react": "^6.1.1",
    "vite": "^8.3.0"
  },
  "scripts": {
    "prebuild": "node --experimental-strip-types scripts/publish.mjs",
    "dev": "npm run prebuild && vite",
    "build": "npm run prebuild && vite build && node scripts/release.mjs",
    "preview": "vite preview",
    "test": "node --test --experimental-strip-types tests/*.test.ts"
  }
}
